'use strict';

/**
 * new-member-survey service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::new-member-survey.new-member-survey');
